# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - marigold

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://index.docker.io/v1/
        username: ${{ secrets.AzureAppService_ContainerUsername_ff461b0e1f3d4a40a708154e51afd40f }}
        password: ${{ secrets.AzureAppService_ContainerPassword_cae50ea7a33f414aafa03ee510703c11 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: index.docker.io/${{ secrets.AzureAppService_ContainerUsername_ff461b0e1f3d4a40a708154e51afd40f }}/webapp:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'marigold'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_20a6d10be90b4c60871a41d9d45a8f87 }}
        images: 'index.docker.io/${{ secrets.AzureAppService_ContainerUsername_ff461b0e1f3d4a40a708154e51afd40f }}/webapp:${{ github.sha }}'